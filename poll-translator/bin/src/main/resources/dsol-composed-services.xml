<?xml version="1.0" encoding="UTF-8"?>
<services>
				
				
	<!-- 
		- The [service-name] is used as the label in the concrete action to link a Service Action to its service.
		- Is possible to have more than one service with the same name, for the case in which one of them fails
		- The [service-id] must be unique!
	 -->			

	<!-- Template for the definition of a SOAP Service -->

	
	<!-- 			
	<service 	type="soap"
				name="[service-name]"
				id="[service-id]"
				wsdl="[wsdl-address]"
				operationNamespaceUri="[operation-namespace-uri]"
				operation="[operation-name]"> -->
		
		<!-- in case of any additional parameter, e.g., an API Key
		<additional-parameter at-index="[parameter-index]" name="[parameter-name]" value="[value]"/>
		  -->
	<!-- 	
	</service>
	 -->
	 
	 <!-- Template for the definition of a Rest Service -->
	<!--
	<service 	type="rest"
				name="[service-name]"
				id="[service-id]"
				url="[endpoint-url]"
				method="[GET|POST|UPDATE|DELETE]"
				mediaType="[application/xml|application/json]"/>
	-->
				
</services>